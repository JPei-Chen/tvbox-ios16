# TVBox-Swift → iOS 16.4 未签名 IPA 打包说明

本目录是 [Jstrom2022/tvbox-Swift](https://github.com/Jstrom2022/tvbox-Swift) 的一个**向下兼容改造版**：
原版要求 iOS 17.0+（用了 SwiftData），本版把最低系统降到 **iOS 16.4**，并通过 GitHub Actions
云编译产出**未签名 IPA**，可直接用巨魔 TrollStore 安装。适合 iOS 16.x + TrollStore 的用户。

---

## 一、我改了什么

| 文件 | 改动 |
| --- | --- |
| `tvbox/Persistence/CacheStore.swift` | **重写**。`VodCollect` / `VodRecord` / `CacheItem` 从 `@Model` 改为 `Codable` 结构体；`CacheStore` 从 SwiftData 的 `ModelContext` 改为 JSON 文件持久化（落在 `Application Support/TVBoxCacheStore/`）。业务方法语义不变，只是去掉了 `context:` 参数。 |
| `tvbox/tvboxApp.swift` | 移除 `ModelContainer` 与 `.modelContainer(...)`。 |
| `tvbox/Views/Detail/DetailView.swift` | 移除 `import SwiftData`、`@Environment(\.modelContext)`，5 处调用去掉 `context:`。 |
| `tvbox/Views/Favorites/FavoritesView.swift` | `@Query` 改为观察 `CacheStore.shared`；删除改为 `store.removeCollect(item)`。 |
| `tvbox/Views/History/HistoryView.swift` | 同上；`清空` 改为 `store.clearHistory()`。 |
| `tvbox/Views/Player/PlayerGestureLayer.swift` | `MagnifyGesture`（iOS 17）→ `MagnificationGesture`（iOS 16）。 |
| 7 个视图文件 | `onChange(of:)` 的**双参数闭包**（iOS 17 专属签名）共 **12 处**全部改为 iOS 16 的单参数写法。 |
| `project.yml` | 4 个 iOS 目标的 `deploymentTarget` 从 `17.0` 改为 `16.4`。 |
| `tvbox.xcodeproj/project.pbxproj` | `IPHONEOS_DEPLOYMENT_TARGET` 同步改为 `16.4`（CI 里会用 XcodeGen 重新生成，这里只是保险）。 |
| `.github/workflows/build-ipa.yml` | **新增**。云编译流水线。 |

> 注：代码里除上述内容外，其余都是 iOS 16 就支持的写法（`ObservableObject` / `NavigationStack` /
> `AVKit` 等），没有 `#Preview` 宏，没有 `@Observable`。

---

## 二、拿到 IPA 的完整步骤

### 1. 前置条件

- 一个 GitHub 账号（免费版即可）。
- 手机：iOS 16.4 + 已装好 TrollStore（巨魔）。

### 2. 把代码传到你自己的仓库

**方式 A：网页上传（不用装 Git）**

1. GitHub 右上角 **+** → **New repository**，名字随意（例如 `tvbox-ios16`），选 **Private** 或 Public 都行，**不要**勾选 "Add a README file"。
2. 进入空仓库 → **uploading an existing file**。
3. 把本目录 `TVBox-iOS16` 里的**所有文件和文件夹**拖进去（注意：`.` 开头的隐藏文件夹 `.github` 也要一并拖入，否则流水线不会出现）。

   > Windows 资源管理器默认不显示 `.github`。可以先在地址栏打开
   > `F:\work\TVBox-iOS16`，然后在文件夹选项里勾选「显示隐藏的文件」，或者直接把
   > `.github` 文件夹单独拖到网页上传框里。
4. 点 **Commit changes**。

**方式 B：命令行（推荐，能确保 `.github` 一起上传）**

```bash
cd /d F:\work\TVBox-iOS16
git init
git add -A
git commit -m "TVBox for iOS 16.4"
git branch -M main
git remote add origin https://github.com/<你的用户名>/tvbox-ios16.git
git push -u origin main
```

### 3. 跑云编译

1. 打开仓库页面 → 顶部 **Actions** 标签。
2. 左侧列表选 **Build unsigned IPA (TrollStore)**。
3. 右侧 **Run workflow** → **Run workflow**。
4. 等它跑完。首次大约 **5~15 分钟**（要下载 VLCKit 的二进制包，比较慢）。

### 4. 下载 IPA

构建成功后，点进那次运行记录，页面底部 **Artifacts** 区域 → 下载 `TVBox-unsigned-ipa`（是个 zip），
解压后得到 **`TVBox-unsigned.ipa`**。

### 5. 巨魔安装

1. 把 `TVBox-unsigned.ipa` 传到手机上（微信/QQ 发送到「文件」、或用爱思助手、iMazing 拖入都行）。
2. 用 **TrollStore** 打开这个 IPA（在文件 App 里长按 → 共享 → 用 TrollStore 打开），
   或者在 TrollStore 里点 **+** 选择该文件。
3. 安装完成后主屏会出现 **TVBox** 图标。

### 6. 填入你的 LunaTV 订阅

1. 打开 App，首次会进入配置页。
2. 在「点播配置地址」里填你的 TVBox 订阅链接，例如：
   `https://tv.900097.xyz/api/tvbox?format=json&filter=off`
   直播地址留空会自动复用点播地址。
3. 保存后进入首页，源列表里会出现 **47 个站点**。

> **已确认的兼容性**：App 的 `Info.plist` 里开启了 `NSAllowsArbitraryLoads`，
> 所以 `http://` 形式的**内网地址**可以直接访问，不用额外配置。
>
> **源类型限制**：本项目只支持 `type=0`（XML）、`type=1`（JSON）、`type=4`（Remote）这类
> CMS 数据源接口；**不支持** `type=3`（蜘蛛 / JAR 动态脚本源）。这是 iOS 平台原生限制，和系统版本无关。

---

## 三、关于 `?url=` 型代理订阅（重要）

订阅里的 47 个站点**全部是 `type=1`**（标准苹果 CMS JSON 接口），App 是支持的。
但每个站点的接口都被包装成了**代理形式**：

```
https://pz.v88.qzz.io/?url=https://iqiyizyapi.com/api.php/provide/vod
```

实测这个代理会把 `url=` **之后的整段**当作目标地址去转发，于是：

| 请求写法 | 实际结果 |
| --- | --- |
| `?url=X&ac=list`（参数拼在外层） | ❌ 被拼成非法路径 → **404** |
| `?url=X`（不带参数） | ⚠️ 永远只返回**首页第一页**，翻页/分类/搜索/详情全失效 |
| `?url=X?ac=videolist&t=26&pg=2`（参数拼进**内层**） | ✅ 正常返回分类列表 |
| `?url=X?ac=detail&ids=84585` | ✅ 正常返回详情 |
| `?url=X?wd=庆余年` | ✅ 正常返回搜索结果 |

原版 App 走的是第一种（错误）写法，所以**直接填这条订阅，点进去会是空的**。
本版已改写 `SourceService.buildURL`：

- 自动识别 `?url=<http(s) 地址>` 型代理（要求存在且仅存在一个 `url` 参数，避免误判普通源）；
- 把 `ac` / `t` / `pg` / `wd` / 筛选参数拼进**内层 URL**；
- 保证 `url` 参数排在 query 的**末尾**（该类代理要求）；
- 其他形状的接口地址完全走原逻辑，不受影响。

分类、翻页、搜索、详情四条链路均已按此规则实测通过。

> **另一个思路**：如果你的 LunaTV 后台有「资源代理 / proxy」之类的开关，关掉它让配置输出
> 源站原始地址，那么原版 App 也能用。但源站可能对 UA / 地区有要求，改 App 更稳。

---

## 四、可能踩到的坑

**1. Actions 构建失败（编译报错）**

我只做了静态代码分析，**没有在真机/Mac 上编译验证过**（我这边是 Windows 环境，编译不了 iOS）。
如果第一次构建出现红色报错，请把报错的那几行（`error: ...`）贴给我，我直接改。

**2. 装到手机后闪退**

- 先确认 TrollStore 设置里点过 **Install ldid**。
- 如果是启动瞬间闪退，通常是二进制兼容问题，把崩溃日志给我看。
- 收藏/历史这类本地数据如果出现异常，检查 `Application Support/TVBoxCacheStore/` 下的 JSON 是否需要清空。

**3. 想改 App 名字 / 包名**

改 `project.yml` 里的 `PRODUCT_NAME` 和 `PRODUCT_BUNDLE_IDENTIFIER`（当前是
`xyz.appinstall.nov.carbon.lam`），改完重新推一次即可。换包名相当于装了一个新 App，
可以和别人编译的版本共存。

**4. 每次都要自己去点构建太麻烦**

想改成「打 tag 自动出包」，或者想把 IPA 直接发到 Release 里，告诉我，我帮你加。

---

## 五、目录结构

```
TVBox-iOS16/
├── .github/workflows/build-ipa.yml   # ★ 云编译流水线
├── project.yml                       # XcodeGen 工程模板（最低系统已改 16.4）
├── tvbox/                            # 源码
│   ├── Persistence/CacheStore.swift  # ★ 已从 SwiftData 改为 JSON 持久化
│   ├── Services/SourceService.swift  # ★ 已适配 ?url= 型代理订阅源
│   ├── Models/ ViewModels/ Views/ Utils/ Assets.xcassets/
├── tvbox-Tests/ tvboxTests/          # 单元测试
├── package_ios.sh / package_mac.sh   # 原作者脚本（需私有证书，本例不用）
└── README.md
```

---

## 五、免责声明（沿用原项目）

本项目仅供编程技术交流与学习 Swift / SwiftUI 开发之用，**不内置、不提供、不分发**任何影视资源或
播放源配置。使用者自行导入的第三方接口所引发的一切版权与法律问题，由使用者自行承担。
